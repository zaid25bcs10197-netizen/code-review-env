[project]
name = "code-review-env"
version = "0.1.0"
description = "OpenEnv Code Review Environment"
authors = [
    { name = "Zaid" }
]
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openai"
]